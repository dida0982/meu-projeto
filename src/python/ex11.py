print("Este código calcula o volume de uma caixa")

comprimento = float(input("Qual o coprimento da caixa ? "))
largura = float(input("Qual a largura da caixa? "))
altura = float(input("Qual a altura da caixa? "))


volume = comprimento * largura * altura

print(f"A caix tem o volume de: {volume:.2f}")
